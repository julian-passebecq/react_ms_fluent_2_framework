import { EntityCard, PageHeader } from '@datapass/ui';
import { practiceCatalog } from '../../../../content/practice';
export default function ReferencePage({ mode, onPractice }: { mode: 'learn' | 'cheatsheets'; onPractice: (track?: string) => void }) {
  return mode === 'learn' ? <><PageHeader eyebrow="LEARNING PATHS" title="A curriculum for working with data" description="24 curriculum tracks, followed by SQL, Python syntax, and practice across data engines." /><div className="sandbox-grid">{practiceCatalog.tracks.map(track => <EntityCard key={track.id} entityId={track.id} title={track.name} description={track.description} eyebrow="Learning path" onSelect={onPractice} />)}</div></>
    : <><PageHeader eyebrow="QUICK REFERENCE" title="Cheat Sheets" description="Review syntax and mental models, then try the examples in your own environment." /><div className="sandbox-references">{practiceCatalog.cheatSheets.map(sheet => <details key={sheet.id}><summary>{sheet.label} — {sheet.subtitle}</summary><ul>{sheet.mentalModels.map(model => <li key={model}>{model}</li>)}</ul>{sheet.sections.map(([title, code]) => <section key={title}><h2>{title}</h2><pre><code>{code}</code></pre></section>)}</details>)}</div></>;
}
