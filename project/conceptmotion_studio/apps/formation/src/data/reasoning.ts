import type { AssessmentSpec, ContentSource, CourseModule, LessonSpec, QuestionSpec } from '@datapass/content';

export interface ReasoningSection { id: string; title: string; text: string; checkpoint: string; visualId?: string }
export interface ReasoningModule { lesson: LessonSpec; courseId: string; sections: readonly ReasoningSection[]; questions: readonly QuestionSpec[]; assessment: AssessmentSpec }
export const reasoningSources: readonly ContentSource[] = [
  { id: 'source.formation.sql-relations', title: 'PostgreSQL table expressions', url: 'https://www.postgresql.org/docs/current/queries-table-expressions.html' },
  { id: 'source.formation.sql-windows', title: 'PostgreSQL window functions', url: 'https://www.postgresql.org/docs/current/tutorial-window.html' },
  { id: 'source.formation.sql-null', title: 'PostgreSQL comparison and NULL predicates', url: 'https://www.postgresql.org/docs/current/functions-comparison.html' },
  { id: 'source.formation.qualify', title: 'BigQuery QUALIFY clause', url: 'https://docs.cloud.google.com/bigquery/docs/reference/standard-sql/query-syntax#qualify_clause' },
  { id: 'source.formation.python-functions', title: 'Python functional programming HOWTO', url: 'https://docs.python.org/3/howto/functional.html' },
];
const sqlQuestions: readonly QuestionSpec[] = [
  { id: 'question.formation.sql-grain', type: 'single-choice', domain: 'sql', prompt: 'The report asks for monthly revenue by customer. What does one output row represent?', options: [{id:'grain',label:'One customer in one calendar month'},{id:'order',label:'One order line'},{id:'customer',label:'One customer across all time'}], correctOptionId:'grain', explanation:'The business question defines the output grain before a query is written.' },
  { id: 'question.formation.sql-cardinality', type: 'single-choice', domain: 'sql', prompt: 'One order matches three order lines. How does an inner join change the row count for that order?', options: [{id:'three',label:'It produces three joined rows'},{id:'one',label:'It always preserves one row'},{id:'zero',label:'It removes the order'}], correctOptionId:'three', explanation:'Each match contributes a row. Summing an order-level amount after this join can triple-count it.' },
  { id: 'question.formation.sql-window', type: 'true-false', domain: 'sql', prompt: 'A window expression alone collapses all detail rows in a partition into one row.', correct:false, explanation:'Aggregation changes grain; a window attaches a result to each surviving detail row.' },
  { id: 'question.formation.sql-null', type: 'single-choice', domain: 'sql', prompt: 'Which predicate deliberately checks for missing values?', options:[{id:'is-null',label:'value IS NULL'},{id:'equals-null',label:'value = NULL'}],correctOptionId:'is-null',explanation:'Ordinary equality with NULL yields unknown. WHERE keeps only true, not unknown.' },
];
const pythonQuestions: readonly QuestionSpec[] = [
  {id:'question.formation.python-pure',type:'single-choice',domain:'python',prompt:'Which function is easiest to replay and test?',options:[{id:'pure',label:'A deterministic transform with explicit inputs and returned outputs'},{id:'io',label:'A transform that also writes files and mutates a global cache'}],correctOptionId:'pure',explanation:'Isolating effects makes transformation behavior reproducible. Perform I/O at explicit boundaries.'},
  {id:'question.formation.python-generator',type:'true-false',domain:'python',prompt:'A generator necessarily holds every produced value in memory at once.',correct:false,explanation:'A generator yields incrementally. A downstream list() or full-data operation can still materialize the data.'},
  {id:'question.formation.python-retry',type:'single-choice',domain:'data-engineering',prompt:'A write may be retried after an uncertain response. What should you design first?',options:[{id:'idempotent',label:'A stable operation key and a repeat-safe write strategy'},{id:'blind',label:'Append the same rows again on every retry'}],correctOptionId:'idempotent',explanation:'Retries alone do not provide idempotency. A key, overwrite boundary or deduplication contract is needed.'},
  {id:'question.formation.python-boundary',type:'true-false',domain:'pyspark',prompt:'A saved PySpark output here demonstrates a live Spark execution in this application.',correct:false,explanation:'The site displays code, explanations and reference output. Spark runs only in an external environment you configure.'},
];
export const reasoningModules: readonly ReasoningModule[] = [
  {
    courseId:'course.dubreu.sql-advanced',
    lesson:{id:'lesson.formation.think-sql',title:'Think in SQL',summary:'Begin with grain. Predict row behavior. Check the result.',objectives:['Define one output row before choosing syntax.','Predict join multiplication and choose aggregation or windows deliberately.'],figureIds:['sql-grain','sql-left-join','sql-group','sql-window-rank'],assessmentIds:['assessment.formation.think-sql'],sourceIds:reasoningSources.filter(s=>!s.id.includes('python')).map(s=>s.id)},
    sections:[
      {id:'question',title:'1. State the question and the grain',text:'Write the requested measure, dimensions, time boundary and inclusion rules. “Revenue by customer and month” is not an order-line dataset. Define a candidate key for the output and decide how missing customers or zero activity should appear.',checkpoint:'Can you complete the sentence “One row represents…” without referring to a SQL keyword?',visualId:'sql-grain'},
      {id:'base',title:'2. Choose a base relation',text:'Start from the population the answer must retain. Compare a sample and a row count before adding joins. A fact table and a dimension describe different kinds of entities; a convenient table is not automatically the correct starting grain.',checkpoint:'Which records must remain even when another relation has no match?'},
      {id:'joins',title:'3. Predict every join',text:'At a unique key, a 1:1 match contributes one pair. A 1:N match fans one record into several. With repeated keys on both sides, N:M creates every matching pair. A left join also retains unmatched left records. Do not use DISTINCT to hide an unexplained multiplication.',checkpoint:'Predict the joined row count, then compare counts and sums before and after.',visualId:'sql-left-join'},
      {id:'filter',title:'4. Filter at the intended stage',text:'WHERE filters individual rows, HAVING filters groups, and dialects such as BigQuery offer QUALIFY after window evaluation. A predicate on the right side of a left join behaves differently in ON and WHERE. NULL comparisons may be unknown; make missing-value policy explicit with IS NULL.',checkpoint:'Is the condition about an input row, a group, or a window result?'},
      {id:'shape',title:'5. Collapse rows—or preserve their detail',text:'GROUP BY produces one result per chosen group. A window retains detail and attaches a calculation over related rows. For a latest-record task, define partition keys and a complete tie-breaking order rather than relying on accidental input order.',checkpoint:'Should the output keep detail rows or change grain?',visualId:'sql-group'},
      {id:'window',title:'6. Project and validate the final shape',text:'Choose the output columns only after the row behavior is clear. Check row count, key uniqueness, NULLs, totals and boundary dates. Compare intermediate results at small checkpoints. The reasoning sequence is not the textual clause order, and neither is a promise about the physical execution plan.',checkpoint:'Does the candidate key still identify exactly one output row?',visualId:'sql-window-rank'},
    ],
    questions:sqlQuestions,
    assessment:{id:'assessment.formation.think-sql',title:'Predict the rows',mode:'practice',questionIds:sqlQuestions.map(q=>q.id),passingScore:75},
  },
  {
    courseId:'course.dubreu.python',
    lesson:{id:'lesson.formation.think-python-de',title:'Think in Python for Data Engineering',summary:'Small transformations, explicit effects, observable contracts.',objectives:['Separate pure transformations from I/O.','Design repeatable processing for empty input, duplicates and schema drift.'],figureIds:['algorithm-scan','de-batching','de-pure-transform','de-retry'],assessmentIds:['assessment.formation.think-python-de'],sourceIds:['source.formation.python-functions']},
    sections:[
      {id:'contract',title:'1. Make the input contract explicit',text:'Specify required fields, types, missing-value rules, keys and expected scale. Validate at the boundary. Decide which errors reject a batch and which records are quarantined with an explanation.',checkpoint:'What happens to empty input, duplicate keys, NULLs and a renamed column?'},
      {id:'structure',title:'2. Choose the data structure for the operation',text:'Use a list for ordered materialized values, a tuple for a fixed record, a dictionary for keyed lookup, and a set for unique membership. A generator supports incremental production; a DataFrame supports tabular operations. Pick the structure from access patterns and memory constraints.',checkpoint:'Do you need repeated random access, keyed lookup, or a single streaming pass?',visualId:'algorithm-scan'},
      {id:'materialize',title:'3. Notice materialization boundaries',text:'Yielding records can limit intermediate memory, but sorting, collecting to a list, or a downstream full-table operation may materialize them. A generator is usually consumed once. Batch only when the receiving system and error policy make the batch meaningful.',checkpoint:'Where is the largest collection created, and who owns its lifetime?',visualId:'de-batching'},
      {id:'pure',title:'4. Compose small deterministic transformations',text:'Pass inputs explicitly and return new results where practical. Keep file, API and database operations at named boundaries. Avoid silently mutating shared lists or dictionaries; aliasing means another caller can observe the same mutation.',checkpoint:'Can you test this transformation without a network, file or global state?',visualId:'de-pure-transform'},
      {id:'retry',title:'5. Design effects for replay',text:'An uncertain response does not tell you whether a write occurred. Use stable operation IDs, deterministic destination keys or a well-defined replacement boundary. Retry transient failures with limits. Idempotency is a property of the operation, not a checkbox on the retry loop.',checkpoint:'If the same batch is processed twice, which result should remain?',visualId:'de-retry'},
      {id:'observe',title:'6. Observe, test and respect the scale boundary',text:'Record input/output counts, elapsed time and categorized failures without logging secrets or entire sensitive records. Test edge cases and output contracts. Python or pandas may fit local data; relational work may belong in SQL, and distributed processing needs its own external runtime. A visual model is not a performance benchmark.',checkpoint:'Which evidence would distinguish bad input, a transformation defect and an unavailable destination?'},
    ],
    questions:pythonQuestions,
    assessment:{id:'assessment.formation.think-python-de',title:'Design a repeatable transform',mode:'practice',questionIds:pythonQuestions.map(q=>q.id),passingScore:75},
  },
];
export function reasoningCourseModule(courseId: string): CourseModule[] {
  return reasoningModules.filter(module=>module.courseId===courseId).map(module=>({id:`module.${module.lesson.id}`,title:'Thinking module',lessons:[module.lesson]}));
}
