# v0.7 handoff note

This directory is the current baseline project. The new architectural requirements live at the handoff root, especially `CODEX_MASTER_PROMPT.md` and `TARGET_ARCHITECTURE.md`.

Do not treat this folder's current single-app layout as the desired final package boundary. Migrate incrementally while preserving working scenes/tests.
