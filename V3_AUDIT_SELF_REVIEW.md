# V3 audit self-review

## Scope and preservation

V3 is a consumer/reuse expansion over baseline `8cccd77ecd0d0b60b1d28ee2e41cffe5ec78a26f`. It does not rebuild the platform. The Linux snapshot repair was independently pushed and hosted-green before consumer implementation. Existing comparison checks and tolerance were not disabled or relaxed.

Preserved gates include pure ConceptMotion core, pure knowledge, semantic-first specs, stable identities, shared provider-neutral workflow semantics, Fluent generic controls, lazy shared Monaco, renderer-neutral Figure contracts, EN/NO infrastructure, reduced motion/fallbacks, legacy code/data and all old tests. Existing V1/V2 local progress keys and IDs remain. Four scaffold presets still generate/type-check/build deterministically.

## Acceptance review

| Area | Review |
| --- | --- |
| Formation | Product renamed in app chrome/path; old lessons preserved; SQL/Python reasoning modules use shared figures, prediction assessments and local progress. |
| Code Sandbox | Full 323-item pinned migration, 500 variants, source metadata/notices retained; old UI and renderers not copied. |
| Code Interview | Separate app/session policy; shared challenge/assessment/progress semantics, local review and flags; code practice is ungraded. |
| Algorithm Atlas | 30 tested semantic adaptations using existing renderer families, shared with Formation/practice. |
| Architecture Atlas | 16 variants across four workloads/provider lenses; stable stage semantics, shared workflow/column-lineage, deterministic radial/layered layouts. |
| Pilot Center | Canonical public registry plus local-only notes/metadata/overlays; shared Project Galaxy; explicit private versus public export. |
| Visual Sandbox | Production route, real shared editor and renderer, bounded invalid-input handling, fallback and export. |
| Storybook / bundles | 38 Golden Gallery stories; static initial routes exclude Monaco; bounded manifest budgets. |
| Coverage | Separate measured/failing floors for six pure packages, including all four newly required packages. No UI-heavy aggregate target. |
| Privacy | Source visibility checked explicitly. Private-source URLs must be excluded from emitted public app/Storybook files, including source maps; opaque attribution and nonbundled audit provenance retain traceability. |

## Findings addressed during implementation

- Missing Linux baseline images: restored from reviewed failed-run artifacts; hosted V2 green proved separately.
- Pilot inline navigation overlap and phone filter density: scoped layout and collapsible filters.
- Submitted Interview answer labels: readable contrast despite disabled controls. Final screenshot review exposed Fluent's higher-specificity disabled rule; a targeted sibling selector and explicit computed-color assertions now verify all six read-only labels while preserving disabled semantics.
- Monaco browser edits: keyboard-focus the actual accessible native edit context instead of the inactive textarea.
- Figure export: unavailable/fallback renderers no longer advertise SVG export; filenames sanitized.
- Mobile scenes: scoped pannable production canvas plus accessible text/captions and keyboard region.
- Windows deterministic corpus: LF attributes prevent checkout conversion from causing byte-drift failures.
- Review mistakes and end-of-sequence behavior: regression tests accompany latest-answer review and frame-boundary corrections.
- Private source repository links: visibility audit drives removal from public runtime output and an emitted-file privacy gate.
- Visual Sandbox resource safety: joins are rejected before multiplicative compilation when candidate pairs/cells exceed the preview budget; table cells, nesting, arrays, frames and UTF-8 input bytes are bounded.
- Multi-figure accessibility: per-instance SVG title/description references avoid same-family collisions in long lessons.
- Hosted Linux phone overflow: native select option widths exposed an intrinsic-width issue in Sandbox filters and Pilot's backup grid. Scoped shrinkable controls and an explicit `minmax(0, 1fr)` grid track address the cause; new fallback-font/control-bound regressions retain all options, Axe checks and the original one-pixel overflow limit.

## Intentional limitations

No private Dubreu corpus was supplied; representative V2 notebooks remain clearly attributed. Visual migrations use authored illustrative inputs, not execution or a full reproduction of each source lesson. Only explicitly mapped practice IDs receive a shared visual. Provider nomenclature is pinned to source snapshots and not live-verified.

All progress/notes are local, without multi-tab conflict resolution or cloud synchronization. Private backups contain private fields and are labeled accordingly. No authentication, backend, runtime, universal judge, Spark/Jupyter execution, pipeline execution, monitoring, mail/news/social integration, DAX/SQL network/parser integration, Data Forge generator, D3 Power BI rewrite or GeoStory was added.

The lazy Monaco/JSON-language payload is still large; it is excluded from non-code entry paths. Seven independently built apps share source packages rather than a deployment-wide CDN cache. Reusable packages are workspace TypeScript entries, not a published npm SDK. Accessibility evidence is automated primary-flow testing and visual/keyboard review, not a formal certification.

## Release evidence

Exact commands, test counts, failures corrected and hosted run conclusions are in [V3_TEST_REPORT.md](V3_TEST_REPORT.md). Completion requires a successful GitHub Actions run on the final pushed main commit; a workflow file or local pass alone is not treated as that evidence.
