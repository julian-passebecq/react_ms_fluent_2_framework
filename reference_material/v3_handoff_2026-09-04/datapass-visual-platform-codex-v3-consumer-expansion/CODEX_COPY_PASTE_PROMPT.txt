Continue `julian-passebecq/react_ms_fluent_2_framework` from the current V2 main branch (baseline 8cccd77ecd0d0b60b1d28ee2e41cffe5ec78a26f or its descendant). Extract this V3 handoff and read START_HERE.md, V2_POST_IMPLEMENTATION_AUDIT.md, V3_SCOPE_AND_BOUNDARIES.md and CODEX_MASTER_PROMPT.md first. Preserve V2; do not rebuild the platform. First fix GitHub Actions run 33913887435's visual-regression issue: all CI stages passed except four Playwright screenshots because Linux baselines were missing. Keep visual tests and get hosted CI green. Then execute V3 as a large consumer/reuse pass: rename the visible learning app to Formation; build Code Sandbox from the existing leetcodedataeng content/pedagogy without copying its old UI; build a separate Code Interview app; build an Algorithm Atlas by migrating 20–30 high-value semantic visuals from the trainer/ML/legacy ConceptMotion into shared Figure/ConceptMotion specs; build Architecture Atlas from architectureweb content through DiagramSpec/WorkflowSpec/lineage rather than another renderer stack; add a reusable deterministic radial/hub layout used by Architecture Atlas and Pilot Center's Project Galaxy; build the local-first Pilot Center with canonical public ProjectRegistry plus structured sticky notes; and add a production Visual Sandbox route in Studio. Formation gets short “Think in SQL” and “Think in Python for Data Engineering” reasoning capstones; PySpark stays display/explanation/external-launch only. Apply the professional Fluent visual language in V3_VISUAL_LANGUAGE.md and reference_assets/formation_visual_direction.png. Do not add Spark/Jupyter/backend/auth/news/mail/social integrations, D3 Power BI rewrite or GeoStory. Run all old/new QA, add pure-V2 package coverage reporting, require hosted CI green, write the requested V3 audit/test/reuse/visual-migration/API/bundle/migration reports, then commit and push main and return the commit SHA + CI run conclusion.
