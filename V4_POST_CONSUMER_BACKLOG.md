# V4 post-consumer backlog

This ledger records evidence; it does not independently authorize implementation. The subsequent user-authorized visual-hardening pass resolves the narrowly identified visual gaps below. Persistence identities, app boundaries and the historical external-consumer baseline remain stable.

| Deferred API / product work | Evidence | Narrow next investigation |
| --- | --- | --- |
| French / arbitrary lexical translations independent of UI locale | Norsk stores French in a deterministic sidecar | A lexical translation map, preserving EN/NO chrome. |
| Entity-neutral mastery/review progress | Norsk's 85-line adapter maps vocabulary into `challenges` | Additive entity learning markers with storage migration/recovery; no universal flashcard engine. |
| Sectioned notebook lesson | Formation composes H1/H3 navigation around shared NotebookCellView | Separate section grouping from lesson presentation policy. |
| Solution-reference metadata | Formation gates 72 applied solution files | Explicit source/solution relationships, preserving identifiers. |
| Safe notebook-media API | Formation bridges source images and unavailable media | Safe origin/media metadata and local discovery; never execute notebooks. |
| Moving SQL window-frame visualization — resolved in shared framework | Formation | Additive `TableWindowFrame` / `windowFrames` on `table.transform`, with current row, consecutive membership, sums and real external FigurePlayer proof. Consumer adoption remains separate. |
| Workflow aggregate readiness / trigger policy | Cloud Architecture branch/skipped flows | Shared provider-neutral policy semantics with truthful static fixtures. |
| Dynamic mapped task instances | Cloud Architecture foreach/backfill | Stable instance identity separate from task definition. |
| Retry/backoff/timeout policy | Cloud Architecture | Typed explanation semantics, not a pipeline executor. |
| Figure selection resolution helper | Cloud Architecture; Portfolio semantic adapters | Pure semantic identity resolution, keeping UI selection state local. |
| Vertical worklist — resolved for DFS; general recursion remains deferred | Visual Algorithms | Generic `collection.flow` shows LIFO push/pop and visited order. No loop-schema hint or new graph engine; arbitrary recursion activation records are not claimed. |
| ProjectRegistry → Galaxy helper | Portfolio and Pilot | Pure registry-to-DiagramSpec mapping only; Pilot notes/private state remain local. |
| Visualize tab when no Figure exists | Code Lab | Review workbench discoverability separately; keep current behavior in this distribution pass. |

The repeated findings implemented now are distribution/bootstrap, a frozen consumer-owned release, canonical-data entrypoints and external scaffold wiring. Further shared APIs need either repeated concrete consumer use or a strong semantic boundary, plus a successful production rerun first.

## Consumer rerun sequence (separate work)

The framework visual pass adds seventeen approved examples, one generic container family, progressive join/sort traces and a shared DOM-motion fix. See [the capability matrix](V4_VISUAL_CAPABILITY_MATRIX.md). None of the six consumer reruns below has been performed as part of this pass. After the final framework pin is green, the visual-priority rerun order is Formation, Visual Algorithms, then Cloud Architecture; Code Lab, Norsk and Portfolio follow for regression adoption. The earlier distribution-only ordering below is retained as historical context.

First update Code Lab, Cloud Architecture and Norsk in place to the new exact framework pin and official bootstrap/release gate, retaining product/content code and committing each consumer's own lockfile. Report before/after friction in each repository's `V4_HARDENING_RERUN.md` and observe its real GitHub Actions result.

Then address Formation's missing notebook/data/media corpus through restored source or deterministic artifact acquisition, render Visual Algorithms' existing three specs through the real shared Figure runtime, and move Portfolio to the external scaffold/release pattern. Compatibility/proxy/static QA remains diagnostic only. None of these six reruns is claimed complete by the framework fixtures.

## V5 remains deferred

No `@datapass/charts`, D3 storytelling, Visual Factory, GeoStory, Power BI generation, D3 → Power BI adapters, backend/auth/cloud sync, Spark/Jupyter execution, universal judge, pipeline execution, SQL parser, VS Code extension or MCP server is included.
